#1、数据转移（中等）
①编解码：根据practice16_101.py中的要求，一步一步完成。

②、读写文件、复制图片（practice16_102.py）：请通过文件读写命令，读取 photo1 里的数据。然后，新建名为“photo2”的图片（在同一个文件夹），写入读到的数据。这样，就通过文件读写的代码，完成了图片的复制（而非鼠标右键）。

③读写间处理数据：课堂上已经见识过文件的一读一写之间，可以对数据进行一定的处理，详见practice16_10301.py，并运行practice16_10301.py。现在续写practice16_10302.py，对得到的“winner”文档再处理一下，让学员的成绩从高到低排列，然后放到新文档“winner_new.txt”。

